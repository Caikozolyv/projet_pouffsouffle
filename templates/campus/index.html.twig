{% extends 'layoutAdmin.html.twig' %}

{% block title %}Campus index{% endblock %}

{% block body %}
    <br>
    <h1 class="text-center">Gérer les campus</h1>
    <br>
    <h3>Filtrer les campus</h3>
    <br>
    <tr class="label">Le nom contient : </tr> <input>    <button class="btn btn-success" type="submit">Rechercher</button>
    <br>
    <br>
    <br>
    <table class="table">
        <thead>
            <tr>

                <th >Nom</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for campus in campus %}
            <tr>

                <td>{{ campus.nom }}</td>

                <td>
                    <a href="{{ path('campus_edit', {'idCampus': campus.id}) }}">Modifier</a>
                    <a href="{{ path('campus_delete', {'idCampus': campus.id}) }}">Supprimer</a>

                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('campus_new') }}">Ajouter</a>
{% endblock %}
