{% extends 'base.html.twig' %}

{% block title %}Campus{% endblock %}

{% block body %}
    <h1>Campus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCampus</th>
                <td>{{ campu.idCampus }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ campu.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('campus_index') }}">back to list</a>

    <a href="{{ path('campus_edit', {'idCampus': campu.idCampus}) }}">edit</a>

    {{ include('campus/_delete_form.html.twig') }}
{% endblock %}
