{% extends 'layout.html.twig' %}

{% block title %}Participant{% endblock %}

{% block body %}
    <h1>Mon Profil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ participant.nom }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ participant.prenom }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ participant.telephone }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ participant.mail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_listing') }}">back to list</a>

    <a href="{{ path('participant_edit_admin', {'idParticipant': participant.idParticipant}) }}">edit</a>

    {{ include('Admin/_delete_form.html.twig') }}
{% endblock %}
